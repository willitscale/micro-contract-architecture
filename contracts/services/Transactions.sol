pragma solidity ^0.4.0;

contract Transactions {
  function _add(uint productId) public returns (bool);
  function _checkout() public returns (uint);
  function _ver() public returns (bytes32);
}
